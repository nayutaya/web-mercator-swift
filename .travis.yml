language: objective-c
osx_image: xcode7
xcode_project: WebMercator.xcodeproj
xcode_scheme: WebMercator
xcode_sdk: iphonesimulator
before_install:
  - brew update
  - brew upgrade xctool || true
  - brew install carthage
  - carthage bootstrap --verbose
